spring.application.name=${SPRING_APP_NAME:springsection16}

spring.config.import=application-prd.properties
spring.profiles.active=default

spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:eazybank}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:root}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}

logging.pattern.console=${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}
logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:trace}

# O SpringBoot não permite que seja definida um tempo menor que 2 minutos (ou 120 segundos) como tempo de duração da sessão
#server.servlet.session.timeout=${SESSION_TIMEOUT:20m}

spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${JWT_SET_URI:http://localhost:9000/oauth2/certs}

#spring.security.oauth2.resourceserver.opaquetoken.introspection-uri=${INSTROSPECT_URI:http://localhost:8180/realms/eazybankdev/protocol/openid-connect/token/introspect}
#spring.security.oauth2.resourceserver.opaquetoken.client-id=${CLIENT_ID:eazybankinstrospect}
#spring.security.oauth2.resourceserver.opaquetoken.client-secret=${CLIENT_SECRET:zkq8lbnFfqssKpxvDkAfPmB3VbUAfeZa}